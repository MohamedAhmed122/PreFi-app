import {Colors, chartPalette} from './colors';
import {width, height, isSmallDevice} from './layouts';
import {fontSizes, fontWeights} from './styles';

export {
  Colors,
  chartPalette,
  width,
  height,
  isSmallDevice,
  fontSizes,
  fontWeights,
};
